# Production API base URL — MUST include the /api/v1 prefix (the backend mounts all routes there).
# Vite inlines this at BUILD time (vite build), so a rebuild/redeploy is required after any change.
VITE_API_URL=https://dynamic-form-api-3bp6.onrender.com/api/v1
